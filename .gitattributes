# Ensure Maven wrapper scripts always use LF line endings (Unix-style)
# This prevents "command not found" errors when running with Git Bash on Windows
**/mvnw text eol=lf
*.sh text eol=lf

# Windows scripts should use CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Default behavior: let Git auto-convert line endings
* text=auto
